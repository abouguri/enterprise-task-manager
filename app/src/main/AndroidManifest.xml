<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".TaskManagerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskManager">

        <!-- Disables WorkManager's default self-initialization (which would otherwise run
             with a plain Configuration before TaskManagerApplication.onCreate() and win over
             the HiltWorkerFactory-based one). Required together with implementing
             Configuration.Provider on TaskManagerApplication - WorkManager.getInstance()
             then lazily initializes itself from that provider on first access instead.
             Confirmed via WorkManagerImpl bytecode, not guessed: without this removal AND
             Configuration.Provider both present, WorkManager throws
             "WorkManager is not initialized properly" at runtime. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TaskManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Required by MSAL to capture the browser redirect after sign-in.
             host/path must match msal_config.json's redirect_uri exactly:
             msauth://<package>/<base64 signature hash>. The hash below is this
             machine's debug keystore signature — it will NOT match on another
             machine or in CI. Regenerate with:
               keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore \
                 -storepass android | openssl sha1 -binary | openssl base64
             then URL-encode it (+ -> %2B, = -> %3D) and update this file and
             msal_config.json together; they must always agree. -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="msauth"
                    android:host="com.jojothemojo.taskmanager"
                    android:path="/QjYXRIRUw%2B1XCa3wk0me3YY9rYw%3D" />
            </intent-filter>
        </activity>
    </application>

</manifest>
